version: '3'
services:
  nts:
    build: .
    volumes:
      - ./private:/etc/ssl/private:ro
    read_only: false
    # Testing only: network_mode: host
    ports:
      # HOST:CONTAINER
      - "4460:4460/tcp"
      # UDP port is not nessescary to solve the challange:
      - "123:123/udp"